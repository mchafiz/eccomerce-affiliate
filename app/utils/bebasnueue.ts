import { Bebas_Neue } from "next/font/google";

const bebasSans = Bebas_Neue({
    weight: ["400"],
    style: "normal",
    display: "swap",
    subsets: ["latin"], // Specify the available subsets
});

export default bebasSans;